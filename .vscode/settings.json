{
  // "editor.theme": "vs-dark",
  /**
   * Отрисовывать вертикальные линии в указанных столбцах.
   * По умолчанию пустой массив.
   */
  // "editor.rulers": [],
  /**
   * Строка, содержащая разделители слов, используемые при навигации по словам.
   * Defaults to `~!@#$%^&*()-=+[{]}\\|;:\'",.<>/?
   */
  // "editor.wordSeparators": "`~!@#$%^&*()-=+[{]}\\\\|;:\\'\",.<>/?",
  /**
   * Включить основной буфер обмена Linux.
   * По умолчанию true.
   */
  // "editor.selectionClipboard": true,
  /**
   * Управлять отображением номеров строк.
   * Если это функция, она будет вызвана при отображении номера строки, и будет отображено возвращаемое значение.
   * В противном случае, если это правда, номера строк будут отображаться нормально (эквивалент использования функции идентификации).
   * В противном случае номера строк не будут отображаться.
   * По умолчанию true.
   */
  // "editor.lineNumbers": "on",
  /**
   * Следует ли выбирать соответствующую строку при нажатии на номер строки?
   * Defaults to true.
   */
  // "editor.selectOnLineNumbers": true,
  /**
   * Управлять шириной номеров строк, резервируя горизонтальное пространство для отображения как минимум количества цифр.
   * Defaults to 5.
   */
  // "editor.lineNumbersMinChars": 5,
  /**
   * Включить рендеринг поля глифа.
   * По умолчанию true в vscode и false в monaco-editor.
   */
  // "editor.glyphMargin": true,
  /**
   * Ширина, зарезервированная для оформления линий (в пикселях).
   * Строчные украшения помещаются между номерами строк и содержимым редактора.
   * Вы можете передать строку в формате с плавающей запятой, за которой следует "ch". например 1.3ch.
   * Defaults to 10.
   */
  // "editor.lineDecorationsWidth": 10,
  /**
   * При открытии курсора к курсору добавляется виртуальный отступ (px), превращающий его в прямоугольник.
   * Это виртуальное заполнение гарантирует, что курсор появится перед тем, как коснуться края области просмотра.
   * Defaults to 30 (px).
   */
  // "editor.revealHorizontalRightPadding": 30,
  /**
   * Сделайте выделение редактора закругленными границами.
   * Defaults to true.
   */
  // "editor.roundedSelection": true,
  /**
   * Управляйте поведением и отрисовкой миникарты.
   */
  // "editor.minimap": {
  //  "enabled": false
  // },
  /**
   * Отображать виджеты переполнения как `fixed`.
   * Defaults to `false`.
   */
  // "editor.fixedOverflowWidgets": false,
  /**
   * Количество вертикальных полос, которые должна отображать линейка обзора.
   * Defaults to 2.
   */
  // "editor.overviewRulerLanes": 2,
  /**
   * Определяет, следует ли рисовать рамку вокруг обзорной линейки.
   * Defaults to `true`.
   */
  // "editor.overviewRulerBorder": true,
  /**
   * Управляйте стилем анимации курсора, возможные значения: 'blink', 'smooth', 'phase', 'expand' и 'solid'.
   * Defaults to 'blink'.
   */
  // "editor.cursorBlinking": "blink",
  /**
   * Увеличьте шрифт в редакторе при использовании колесика мыши в сочетании с удержанием Ctrl.
   * Defaults to false.
   */
  // "editor.mouseWheelZoom": false,
  /**
   * Управляйте стилем курсора: 'block' или 'line'.
   * Defaults to 'line'.
   */
  // "editor.cursorStyle": "line",
  /**
   * Управляйте шириной курсора, когда для cursorStyle установлено значение 'line'
   */
  // "editor.cursorWidth": 2,
  /**
   * Включите лигатуры шрифтов.
   * Defaults to false.
   */
  // "editor.fontLigatures": false,
  /**
   * Отключите использование `will-change` для слоев полей и линий редактора.
   * Использование `will-change` действует как подсказка браузерам создать дополнительный уровень.
   * Defaults to false.
   */
  // "editor.disableLayerHinting": false,
  /**
   * Отключите оптимизацию для моноширинных шрифтов.
   * Defaults to false.
   */
  // "editor.disableMonospaceOptimizations": false,
  /**
   * Если курсор скрыт на обзорной линейке.
   * Defaults to false.
   */
  // "editor.hideCursorInOverviewRuler": false,
  /**
   * Разрешите прокрутку на один размер экрана после последней строки.
   * Defaults to true.
   */
  // "editor.scrollBeyondLastLine": true,
  /**
   * Включите эту функцию, чтобы прокрутка могла выходить за пределы последнего столбца на несколько столбцов.
   * Defaults to 5.
   */
  // "editor.scrollBeyondLastColumn": 5,
  /**
   * Включите, чтобы редактор анимировал прокрутку до позиции.
   * Defaults to false.
   */
  // "editor.smoothScrolling": false,
  /**
   * Включите, чтобы редактор устанавливал интервал, чтобы проверять, изменился ли размер его dom-узла контейнера.
   * Включение этого может серьезно повлиять на производительность.
   * Defaults to false.
   */
  // "editor.automaticLayout": false,
  /**
   * Управляйте упаковкой редактора.
   * When `wordWrap` = "off", линии никогда не будут переноситься.
   * When `wordWrap` = "on", линии будут переноситься по ширине области просмотра.
   * When `wordWrap` = "wordWrapColumn", строки будут перенесены на `wordWrapColumn`.
   * When `wordWrap` = "bounded", линии будут перенесены на min (ширина области просмотра, wordWrapColumn).
   * Defaults to "off".
   */
  // "editor.wordWrap": "on",
  /**
   * Управляйте упаковкой редактора.
   * When `wordWrap` = "off", линии никогда не будут переноситься.
   * When `wordWrap` = "on", линии будут переноситься по ширине области просмотра.
   * When `wordWrap` = "wordWrapColumn", строки будут перенесены на `wordWrapColumn`.
   * When `wordWrap` = "bounded", линии будут перенесены на min (ширина области просмотра, wordWrapColumn).
   * Defaults to 80.
   */
  // "editor.wordWrapColumn": 0,
  /**
   * Принудительный перенос слов, если текст выглядит как минифицированный/сгенерированный файл.
   * Defaults to true.
   */
  // "editor.wordWrapMinified": true,
  /**
   * Управляйте отступом переносимых строк. Может быть: 'none', 'same', 'indent' или 'deepIndent'.
   * Defaults to 'same' in vscode and to 'none' in monaco-editor.По умолчанию в vscode установлено 'same', а в monaco-редакторе - 'none'.
   */
  // "editor.wrappingIndent": "same",
  /**
   * Настроить символы переноса слов. Перед этими персонажами будет сделана пауза.
   * Defaults to '{([+'.
   */
  // "editor.wordWrapBreakBeforeCharacters": "{([+",
  /**
   * Настроить символы переноса слов. После этих символов будет введен перерыв.
   * Defaults to ' \t})]?|&,;'.
   */
  // "editor.wordWrapBreakAfterCharacters": " \t})]?|&,;",
  /**
   * Настроить символы переноса слов. Разрыв будет введен после этих символов только в том случае, если не было найдено `wordWrapBreakBeforeCharacters` или `wordWrapBreakAfterCharacters`.
   * Defaults to '.'.
   */
  // "editor.wordWrapBreakObtrusiveCharacters": ".",
  /**
   * Защита производительности: прекратить отображение строки после x символов.
   * Defaults to 10000.
   * Используйте -1, чтобы никогда не останавливать рендеринг
   */
  // "editor.stopRenderingLineAfter": -1,
  /**
   * Настройте зависание редактора.
   */
  // "editor.hover": {
  /**
   * Включите наведение.
   * Defaults to true.
   */
  // "enabled": true,
  /**
   * Задержка отображения зависания.
   * Defaults to 300.
   */
  // "delay": 300,
  /**
   * Является ли наведение липким, чтобы его можно было щелкнуть и выбрать его содержимое?
   * Defaults to true.
   */
  // "sticky": true
  // },
  /**
   * Включите обнаружение ссылок и сделайте их интерактивными.
   * Defaults to true.
   */
  // "editor.links": true,
  /**
   * Включите встроенные декораторы цвета и отрисовку средства выбора цвета.
   */
  // "editor.colorDecorators": true,
  /**
   * Включить настраиваемое контекстное меню.
   * Defaults to true.
   */
  // "editor.contextmenu": true,
  /**
   * A multiplier to be used on the `deltaX` and `deltaY` of mouse wheel scroll events.
   * Defaults to 1.
   */
  // "editor.mouseWheelScrollSensitivity": 1,
  /**
   * The modifier to be used to add multiple cursors with the mouse.
   * Defaults to 'alt'
   */
  // "editor.multiCursorModifier": "alt",
  /**
   * Merge overlapping selections.
   * Defaults to true
   */
  // "editor.multiCursorMergeOverlapping": true,
  /**
   * Configure the editor's accessibility support.
   * Defaults to 'auto'. It is best to leave this to 'auto'.
   */
  // "editor.accessibilitySupport": "auto",
  /**
   * Suggest options.
   */
  // "editor.suggest": {
  /**
   * Enable graceful matching. Defaults to true.
   */
  // "filterGraceful": true,
  /**
   * Prevent quick suggestions when a snippet is active. Defaults to true.
   */
  // "snippetsPreventQuickSuggestions": true
  // },
  /**
   * Enable quick suggestions (shadow suggestions)
   * Defaults to true.
   */
  // "editor.quickSuggestions": true,
  /**
   * Quick suggestions show delay (in ms)
   * Defaults to 500 (ms)
   */
  // "editor.quickSuggestionsDelay": 500,
  /**
   * Parameter hint options.
   */
  // "editor.parameterHints": true,
  /**
   * Render icons in suggestions box.
   * Defaults to true.
   */
  // "editor.iconsInSuggestions": true,
  /**
   * Options for auto closing brackets.
   * Defaults to language defined behavior.
   */
  // "editor.autoClosingBrackets": true,
  /**
   * Enable auto indentation adjustment.
   * Defaults to false.
   */
  // "editor.autoIndent": false,
  /**
   * Enable format on type.
   * Defaults to false.
   */
  // "editor.formatOnType": false,
  /**
   * Enable format on paste.
   * Defaults to false.
   */
  // "editor.formatOnPaste": false,
  /**
   * Enable format on save.
   * Defaults to true.
   */
  // "editor.formatOnSave": true,
  /**
   * Controls if the editor should allow to move selections via drag and drop.
   * Defaults to false.
   */
  // "editor.dragAndDrop": false,
  /**
   * Enable the suggestion box to pop-up on trigger characters.
   * Defaults to true.
   */
  // "editor.suggestOnTriggerCharacters": true,
  /**
   * Accept suggestions on ENTER.
   * Defaults to 'on'.
   */
  // "editor.acceptSuggestionOnEnter": "on",
  /**
   * Accept suggestions on provider defined characters.
   * Defaults to true.
   */
  // "editor.acceptSuggestionOnCommitCharacter": true,
  /**
   * Enable snippet suggestions. Default to 'true'.
   */
  // "editor.snippetSuggestions": "top",
  /**
   * Copying without a selection copies the current line.
   */
  // "editor.emptySelectionClipboard": true,
  /**
   * Enable word based suggestions. Defaults to 'true'
   */
  // "editor.wordBasedSuggestions": true,
  /**
   * The history mode for suggestions.
   */
  // "editor.suggestSelection": "recentlyUsedByPrefix",
  /**
   * Включить выделение выделения.
   * По умолчанию true.
   */
  // "editor.selectionHighlight": true,
  /**
   * Включить выделение семантических вхождений.
   * По умолчанию true.
   */
  // "editor.occurrencesHighlight": true,
  /**
   * Показать код линзы
   * По умолчанию true.
   */
  // "editor.codeLens": true,
  /**
   * Управляйте поведением и отображением лампочки действия кода.
   */
  // "editor.lightbulb": {
  //   "enabled": true
  // },
  /**
   * Включить сворачивание кода
   * По умолчанию true.
   */
  // "editor.folding": true,
  /**
   * Выбирает стратегию складывания. 'auto' использует стратегии, представленные для текущего документа, 'indentation' использует стратегию сворачивания на основе отступов.
   * По умолчанию 'auto'.
   */
  // "editor.foldingStrategy": "auto",
  /**
   * Определяет, будут ли действия сгиба в желобе всегда оставаться видимыми или скрытыми, если указатель мыши не находится над желобом.
   * По умолчанию "наведение курсора". 'mouseover'.
   */
  // "editor.showFoldingControls": "mouseover",
  /**
   * Включить выделение совпадающих скобок.
   * По умолчанию true.
   */
  // "editor.matchBrackets": true,
  /**
   * Включить рендеринг пробелов.
   * По умолчанию нет.
   */
  // "editor.renderWhitespace": "none",
  /**
   * Включить рендеринг управляющих символов.
   * По умолчанию - false.
   */
  // "editor.renderControlCharacters": false,
  /**
   * Включите отображение направляющих отступа.
   * По умолчанию true.
   */
  // "editor.renderIndentGuides": true,
  /**
   * Включить выделение активной направляющей отступа.
   * По умолчанию true.
   */
  // "editor.highlightActiveIndentGuide": true,
  /**
   * Включить рендеринг выделения текущей строки.
   * По умолчанию все.
   */
  // "editor.renderLineHighlight": "all",
  /**
   * Вставка и удаление пробелов выполняется после позиций табуляции.
   */
  // "editor.useTabStops": true,
  /**
   * Семейство шрифтов
   */
  // "editor.fontFamily": "Fira Code, Menlo, Monaco, 'Courier New', monospace",
  /**
   * Толщина шрифта
   */
  // "editor.fontWeight": "normal",
  /**
   * Размер шрифта
   */
  // "editor.fontSize": 12,
  /**
   * Высота строки
   */
  // "editor.lineHeight": 18,
  /**
   * Расстояние между буквами
   */
  // "editor.letterSpacing": 0,
  /**
   * Управляет исчезновением неиспользуемых переменных.
   */
  // "editor.showUnused": true
}
